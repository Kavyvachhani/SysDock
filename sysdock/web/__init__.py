"""SysDock built-in web surface: dashboard, JSON API, SSE stream, optional /metrics.

Self-contained (stdlib only) and reads from the one shared snapshot. See
:mod:`sysdock.web.server` and :mod:`sysdock.web.auth`.
"""
